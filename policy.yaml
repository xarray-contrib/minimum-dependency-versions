exclude:
  - coveralls
  - pip
  - pytest
  - pytest-asyncio
  - pytest-cov
  - pytest-env
  - pytest-mypy-plugins
  - pytest-timeout
  - pytest-xdist
  - pytest-hypothesis
channels:
  - conda-forge
platforms:
  - noarch
  - linux-64
# in case conda-forge release dates are not accurate enough
overrides: {}
policy:
  # all packages in months
  packages:
    python: 30
    numpy: 18
  default: 12
  ignored_violations:
    - pydap
